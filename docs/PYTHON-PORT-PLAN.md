# Python port plan

**Status: plan only. No Python code exists yet; nothing here is implemented.**

This records the decisions already made about a native Python implementation of
`ag-ui-validate`, the reasoning behind them, and the concrete inventory/design work
needed to execute the port when it starts. It does not start the port.

## Decisions already made (not up for relitigation)

- **Native Python reimplementation**, not a wrapper that shells out to the Node CLI
  (a linter shouldn't drag a second runtime into slim containers and CI images) and not
  transpiled or a shared Rust/WASM core (both destroy the readable, zero-dep,
  easy-to-contribute property that is the reason a community adopts a conformance tool).
- **Same repo.** `spec/` ([catalog.json](../spec/catalog.json),
  [fixtures/](../spec/fixtures/)) is already shared, top-level, and language-neutral
  (see the M0 restructure). When the port starts, `src/` moves to `js/` and Python lands
  in `py/`. `action/` stays at the repo root permanently — GitHub Action convention, and
  it invokes the CLI as a black box regardless of which language builds it.
- **Naming:** PyPI `ag-ui-validate`, import package `ag_ui_validate`, console script
  `ag-ui-validate` — so `uvx ag-ui-validate <url>` and `npx ag-ui-validate <url>` are the
  same command with the same basic output.
- **Correctness contract is the shared fixture corpus, not shared code.** Both
  implementations run the identical `spec/fixtures/` corpus and must emit identical
  diagnostics. A CI parity job runs both across every fixture and diffs — the drift guard,
  and also what makes a third-party implementation possible (see §5).
- **The pytest plugin is the priority surface, more than the CLI.** Nearly every AG-UI
  *server* is Python (LangGraph, CrewAI, Google ADK, Pydantic AI, Agno, LlamaIndex), so
  Python is where the non-conformant emitters actually live. Landing in framework
  maintainers' own integration suites is the goal.
- **Timing:** the port starts after `spec/catalog.json` has stabilized against real
  captured streams. Porting a churning ruleset doubles the cost of every rule change.

---

## 1. Port inventory (file-by-file across `src/`)

Legend: **mechanical** = direct translation, same shape and control flow; **redesign** =
same behavior, idiomatic Python looks structurally different; **generated** = not
hand-ported, regenerated by new Python-side tooling; **JS-only** = no Python counterpart
needed (a different artifact fills its role).

| File | Lines | Classification | Notes |
|---|---:|---|---|
| `src/index.ts` | 465 | redesign | The `createValidator()` closure-returning-object-literal is a class in disguise — the natural Python shape is a `Validator` class with `feed`/`finalize`/`report`/`emit_external`/`mark_skipped` methods and instance state (`self.diagnostics`, `self.run`, …) instead of captured closure variables. The algorithm itself (event dispatch, run-scope tracking, terminal-event handling) is a line-for-line port; only the *container* changes. |
| `src/types.ts` | 136 | mechanical | `Diagnostic`/`Report`/`ValidatorOptions`/etc. → `dataclass`es or `TypedDict`s; `CanonicalFeature` → `Literal[...]` or `enum.Enum`. |
| `src/rules/catalog.ts` | 84 | mechanical | Loader + `validate_catalog()` port directly; reads the *same* `spec/catalog.json`, no per-language copy. |
| `src/rules/checks/context.ts` | 117 | mechanical | `RunState`/`StreamState`/`CheckApi` → dataclasses; `Map`→`dict`, `Set`→`set`. Already the most "functional-style" file in the codebase (explicit state passed in, no hidden closures), which is exactly what makes it port cleanly. |
| `src/rules/checks/lifecycle.ts` | 59 | mechanical | |
| `src/rules/checks/state.ts` | 72 | mechanical | |
| `src/rules/checks/reasoning.ts` | 97 | mechanical | |
| `src/rules/checks/text.ts` | 109 | mechanical | |
| `src/rules/checks/toolcalls.ts` | 167 | mechanical | The `switch (type) { case "TOOL_CALL_START": ... }` dispatch is a good candidate for Python 3.10+ `match`/`case`, but a plain `if/elif` chain is just as faithful and doesn't force a version floor decision on this file alone. |
| `src/rules/checks/transport.ts` | 17 | mechanical | |
| `src/protocol/jsonpatch.ts` | 220 | mechanical | Hand-rolled RFC 6901/6902 over plain objects/arrays — no JS-specific tricks (see §3). Recursive `dict`/`list` port, same shape throughout including `locate()`'s tri-state return and the atomic clone-then-apply strategy. |
| `src/protocol/event-table.ts` | 316 | generated | This file is *itself* generated output (from `scripts/generate-event-table.mjs` against `@ag-ui/core`'s zod schemas) — it is not hand-written and not "ported." Python needs its own generator deriving the same shape from `ag_ui.core`'s **pydantic** models (`ag-ui-protocol` on PyPI — confirmed to exist, pydantic v2-based, `requires_python >=3.9`). See §2. |
| `src/transport/index.ts` | 331 | mechanical | `validateBody`/`validateEndpoint` port directly onto `asyncio`. The `Symbol.asyncIterator in body` duck-typing branch (ReadableStream vs. AsyncIterable) has no Python analogue and simply disappears — Python's async-iterator protocol is uniform, so this *simplifies* in the port rather than complicating it. |
| `src/transport/sse.ts` | 126 | mechanical | Incremental UTF-8 decoding (`TextDecoder(..., {stream:true})`) maps to `codecs.getincrementaldecoder("utf-8")`; the line-buffering/CRLF-holdback state machine is a direct port. Async generators are native Python. |
| `src/transport/ndjson.ts` | 25 | mechanical | Trivial. |
| `src/report/index.ts` | 10 | mechanical | Becomes `ag_ui_validate/report/__init__.py` re-exports. |
| `src/report/json.ts` | 20 | mechanical | |
| `src/report/junit.ts` | 56 | mechanical | String building + XML escaping, no library needed. |
| `src/report/pretty.ts` | 102 | mechanical | ANSI codes are platform-agnostic; TTY detection via `sys.stdout.isatty()`. |
| `src/report/sarif.ts` | 109 | mechanical | Plain dict construction. |
| `src/cli-args.ts` | 213 | mechanical | Resolved (§8 open question 3, before PM6 started): ported the hand-rolled `Flag` table directly rather than moving to `argparse`/`click`, for byte-parity with the TS CLI's flags and messages. |
| `src/cli.ts` | 152 | mechanical | Node API swap only: `node:fs`→`pathlib`/`open()`, `process.argv`→`sys.argv`, `process.stdin`→`sys.stdin.buffer`, top-level `main().then(...)`→`sys.exit(asyncio.run(main()))`. Structure is unchanged. |
| `src/vitest/matcher.ts` | 77 | mechanical | The pure `toBeValidAGUI(received, options) -> {pass, message}` logic is a direct port; it becomes the core of the pytest plugin's assertion helper (§6). |
| `src/vitest/index.ts` | 19 | JS-only | This is purely vitest's `expect.extend` registration glue — pytest has a completely different extension mechanism (entry points, not `expect.extend`). Not ported; replaced by `ag_ui_validate/pytest_plugin.py`, a new artifact (§6). |

**Total: 3,099 lines across `src/`.** The overwhelming majority (≈2,900 lines / 21 of 23
files) is mechanical or a light structural redesign — this codebase is unusually
port-friendly because it was already written data-driven (the catalog is JSON) and
avoids TS type-level machinery in favor of plain discriminated unions, `Map`/`Set`, and
explicit state objects. See §2 for where the real difficulty actually lives — it isn't
in `src/`.

---

## 2. What will be awkward — and what to change now

Honest finding: **there is very little in `src/` itself that resists a clean port.** No
generic type-level machinery, no structural-typing tricks, no clever conditional types —
the `noUncheckedIndexedAccess`/`exactOptionalPropertyTypes`/`verbatimModuleSyntax`
strictness sprinkled through the code (non-null assertions, `export type` splits) is pure
TypeScript-compiler ceremony that simply doesn't exist as a category of problem in
Python. The `createValidator` closure pattern (§1) is the only real "shape" translation,
and it's a clean, unambiguous one (closure → class).

The genuine difficulty is **not in `src/` — it's in the tooling that isn't hand-written
source**:

1. **Zod-schema introspection in `scripts/derive-lib.mjs` + `scripts/generate-event-table.mjs`
   (the `unwrap`/`fieldSpec`/`describe` functions).** These walk zod's internal `_def`
   representation (`ZodOptional`, `ZodDefault`, `ZodDiscriminatedUnion`, …) — entirely
   TS/zod-specific and not reducible by changing the TS code, because it's inherently
   tied to zod's object model. The Python side needs a **from-scratch** equivalent
   walking **pydantic v2** models instead (`model_fields`, `FieldInfo.annotation`,
   `typing.get_origin`/`get_args`, `Literal`, discriminated unions via
   `Field(discriminator=...)`). This isn't a port — it's new tooling built against a
   different SDK (`ag-ui-protocol` / `ag_ui.core` on PyPI, confirmed pydantic-based).
2. **`src/vitest/index.ts`'s `expect.extend` registration** has no pytest equivalent
   in kind — pytest plugins register via entry points and hook functions, not a global
   matcher registry. Replaced wholesale by a new pytest plugin module, not translated.

**Done, bundled into PM1:** the `CATEGORY` and `DEPRECATED` maps in
`generate-event-table.mjs` were hand-maintained plain JS objects (wire-type → category
string; wire-type → replacement name for `@deprecated` markers) — *data*, not
zod-specific logic, exactly the kind of language-neutral fact `spec/` was created in the
M0 restructure to hold. They're now [`spec/event-categories.json`](../spec/event-categories.json),
read by both `generate-event-table.mjs` and the new `py/scripts/generate_event_table.py`
(§7, PM1), instead of two hand-maintained copies drifting apart. Regenerating
`event-table.ts` after the hoist produced a byte-identical file — confirmed zero
behavior change on the TS side.

That hoist had a second payoff, also landed: `check-spec-links.mjs` used to import
`EVENT_TABLE` from the built JS `dist/` purely to read each event's `specUrl` — but
every one of those URLs turned out to be a *deterministic function* of the wire type
string itself (`EVENTS_DOC` + `wireType.toLowerCase().replace(/_/g, "")`, with a
category-based override for the deprecated `thinking` group), verified by diffing this
computation against all 33 entries of the built `EVENT_TABLE` (0 mismatches). So
`check-spec-links.mjs` no longer needs `dist/`, any SDK, or either language's build
output at all — it moved to the root [`scripts/`](../scripts/check-spec-links.mjs)
alongside `generate-rule-docs.mjs`, and Python never needs its own copy. The other five
`js/scripts/` tools (`build-fixtures.mjs`, `demo.mjs`, `fuzz.mjs`, `e2e-live-server.mjs`,
and the zod-introspection trio) don't have this out: they each test *this
implementation's* validator/transport/build against real behavior, so Python needs its
own independently-written equivalents (per the milestones below), not a shared file.

Everything else genuinely is a straightforward, low-risk translation.

---

## 3. Dependency budget

Target: **zero runtime dependencies** for the importable core, matching the TS side.
Three cases were named as likely-hard; a fourth turned out to be the real hard one.

| Case | Verdict | Reasoning |
|---|---|---|
| RFC 6902 JSON Patch application | **Vendor / hand-port, zero-dep** | `src/protocol/jsonpatch.ts` is ~220 lines of plain recursive dict/list traversal with no JS-specific behavior (see §1) — a direct, low-risk hand-port, same as the TS side chose. |
| SSE frame parsing | **Vendor / hand-port, zero-dep** | Python's `codecs.getincrementaldecoder("utf-8")` covers `TextDecoder(..., {stream:true})`; async generators are native. No library needed. |
| JSON Schema validation | **N/A — not actually used** | The TS "schema" check (`validateSchema` in `src/index.ts`, driven by `EventSpec.fields`) is a hand-rolled subset (`kind`/`required`/`enum`), not real JSON Schema — no `$ref`, `oneOf`, `allOf`, etc. It ports directly with zero dependencies; there is nothing here to replace. |
| **Async HTTP client for `validateEndpoint`** | **Real dependency, resolved: `httpx`, as an optional `[transport]` extra** | Not one of the three named cases, but the actual hard one. Node/browsers ship `fetch` as a *platform global* — that's why `src/transport/index.ts` has zero added runtime dependencies today; it's an accident of platform, not a discipline the TS code enforces. Python's standard library has no async, streaming-capable HTTP client. `httpx` exposes `response.aiter_bytes()`, giving access to raw chunk boundaries as they arrive — exactly what AGUI506 (keepalive gaps) and AGUI507 (buffered/unflushed responses) need to measure. Decided (§8, resolved before PM5 started): depend on `httpx` for `ag_ui_validate.transport` and the CLI's URL path, imported lazily, scoped to `pip install ag-ui-validate[transport]` rather than a required dependency — the importable core and the pytest plugin's *recorded-stream* path (the primary pytest-plugin use case — asserting on captured events, not live endpoints) stay genuinely dependency-free on a bare install. This is a real, not cosmetic, parity gap with the TS side's "zero deps" claim, and the one place it can't be closed like-for-like. |

Devlopment-only dependencies (mirroring `@ag-ui/core`/`vitest`/`tsdown`/`typescript` as
devDependencies on the TS side — never shipped):

- **`ag-ui-protocol`** (PyPI; import name `ag_ui`) — the official pydantic-based Python
  AG-UI SDK. Devdependency for the event-table derivation script and the
  sdk-alignment-equivalent tests (mirrors `@ag-ui/core`'s role exactly). `requires_python
  >=3.9`, depends on `pydantic>=2.11.2`.
- **`pytest`** — devDependency for running the suite, and the pytest-plugin's runtime
  peer (mirrors `vitest` being a `peerDependency` here).
- **`hatchling`** — build backend (§4).
- **A type checker** — see the open question in §8 (`pyright` vs. `mypy`).

---

## 4. Packaging

- **Build backend: `hatchling`**, with `spec/` force-included into the wheel so the
  catalog and fixture corpus ship inside the installed package — the same requirement
  the M0 restructure's `npm pack --dry-run` check enforces on the TS side. From
  `py/pyproject.toml`:

  ```toml
  [tool.hatch.build.targets.wheel.force-include]
  "../spec" = "ag_ui_validate/spec"
  ```

  Read at runtime via `importlib.resources.files("ag_ui_validate") / "spec" /
  "catalog.json"` — unlike the TS build, where tsdown *inlines* the JSON into the bundle
  at compile time, Python reads the shipped file at import time. Both approaches ship
  `spec/` in the distributable artifact; only the mechanism differs.

- **Console script:**

  ```toml
  [project.scripts]
  ag-ui-validate = "ag_ui_validate.cli:main"
  ```

  `main()` is a sync wrapper (`sys.exit(asyncio.run(_amain()))`) around the async CLI
  body, matching `src/cli.ts`'s `main().then(...)` shape.

- **Python version floor.** `ag-ui-protocol` requires `>=3.9`, but **3.9 is already
  past end-of-life as of this writing (2026-08-17; EOL was 2025-10)**, and 3.10 is close
  behind (EOL 2026-10). This mirrors exactly the reasoning behind the TS side's own
  `engines: ">=22"` decision (Node 20 EOL, not a technical Node-22-API requirement — see
  the M0 follow-up audit). Recommendation: **apply the same policy, not a fixed number**
  — target the oldest non-EOL CPython version at actual port kickoff time. As of today
  that would be **3.11** (EOL 2027-10); confirm against the current Python release
  calendar when the port actually starts, since "starts after the catalog stabilizes"
  means this could be many months out.

---

## 5. Parity CI design

Both implementations independently assert, in their own native test suites, that their
diagnostics equal `spec/fixtures/*/expected.json` — `test/fixtures.test.ts` on the TS side,
`py/tests/test_fixtures.py` (PM4, done) on the Python side. If both are green, they are
transitively equal to each other *for the fields both check byte-for-byte*. That alone is
*necessary* but not the strongest possible check — it doesn't exercise either shipped CLI,
only each language's internal validator call.

**Known, permanent exceptions the diff job must carry forward, not treat as regressions**
(both documented in `test_fixtures.py` and PM4's plan row above): `AGUI204`/`AGUI502`'s
`message` embeds each runtime's native JSON-parser error text, which will never match
word-for-word between V8 and Python's `json` module; `AGUI503`'s `message` embeds
`{sdkVersion}`, which will never match between `@ag-ui/core` and `ag-ui-protocol` since
they're versioned independently. A naive full-diagnostic-array `toEqual`-style diff would
flag all three on every single run. The CLI-to-CLI diff job needs the same
rule/severity/pointer/eventIndex-exact-but-message-tolerant comparison `test_fixtures.py`
already implements, not a blanket deep-equal.

**Recommended design: a direct CLI-to-CLI diff job**, not just "both test suites are
green":

- For every fixture in `spec/fixtures/` (both `stream.jsonl` and `scenario.json`
  variants), run it through **both shipped CLIs** with `--json` output
  (`node dist/cli.js --json <file>` and `uvx ag-ui-validate --json <file>` /
  `python -m ag_ui_validate --json <file>`, from a built wheel — not `python -m pytest`
  internals) and deep-diff the resulting `diagnostics` arrays.
- This is strictly more valuable than comparing each language's unit-test assertions,
  because it also exercises the actual packaging/CLI-wiring path (argument parsing,
  reporter formatting, exit codes) — a bug there wouldn't show up in either language's
  own fixture-runner test, only in a real cross-CLI invocation.
- **Failure output:** a per-fixture table — fixture path, TS diagnostics, Python
  diagnostics, and a structural diff — posted as the job's summary (GitHub Actions job
  summary markdown), not just a pass/fail flag, so a maintainer can see exactly which
  rule/field diverged without re-running locally.
- **Cadence** (mirroring the pattern already shipped for the SDK-drift job, §"related
  work" below): run on every PR that touches `spec/**`, `js/src/**`, or `py/**` (path
  filters keep this off unrelated doc/README PRs), *plus* a nightly scheduled run as a
  backstop against anything that slips through path-filter gaps or environment drift.
  Confirm this cadence in §8 — a stricter "PR-only" or looser "nightly-only" policy are
  both reasonable alternatives depending on how much CI cost the corpus's size ends up
  costing (currently 47 fixtures; will grow as both catalogs grow).

**Related work already shipped:** `.github/workflows/sdk-drift.yml` (added as part of
this same round of follow-up work) establishes the pattern this job should reuse — open
a PR on a clean, actionable diff; file a deduplicated issue when the check itself can't
run cleanly (there, a zod-derivation failure; here, e.g. a CLI that fails to build).

---

## 6. pytest plugin API sketch

**Registration** — a standard `pytest11` entry point, so the plugin activates on install
with no explicit import needed in the user's test files (simpler than the vitest side,
which requires `import "ag-ui-validate/vitest"` in a setup file):

```toml
[project.entry-points.pytest11]
ag_ui_validate = "ag_ui_validate.pytest_plugin"
```

**Assertion helper.** Pytest idiom favors `assert helper(...)` with pytest's assertion
rewriting for boolean-returning helpers, but the interesting content here is a
*multi-line diagnostic message* (mirroring the vitest matcher's `message()` — every
violated rule, its spec citation) that we want fully custom, not autogenerated. The
better fit is an `assert_*`-style helper that raises with a rich message on failure and
returns `None` on success (the same shape as `pandas.testing.assert_frame_equal` or
`pytest.approx`-adjacent conventions), not a boolean matcher:

```python
from ag_ui_validate.pytest_plugin import assert_valid_agui

def test_my_agent_stream(captured_events):
    assert_valid_agui(captured_events, features=["shared-state"], max_warnings=0)
```

`assert_valid_agui(received, *, features=None, severity_overrides=None,
max_warnings=None)` accepts the same shapes the TS matcher does — a list of event
dicts/JSON strings, or a JSONL string — and raises `AssertionError` with the joined
per-diagnostic lines (rule, severity, spec URL) when invalid.

**Validating a live endpoint from a test** — an async counterpart, usable directly with
pytest's native async support (or `pytest-asyncio`):

```python
from ag_ui_validate.pytest_plugin import assert_valid_agui_endpoint, validate_agui_endpoint

async def test_my_live_agent():
    await assert_valid_agui_endpoint("http://localhost:8000/agui", max_warnings=0)

async def test_inspect_report():
    report = await validate_agui_endpoint("http://localhost:8000/agui")
    assert report.summary.errors == 0
    assert report.features["shared-state"] == "exercised"
```

`validate_agui_endpoint` returns the full `Report` (for tests that want to inspect
feature-matrix or summary detail beyond pass/fail); `assert_valid_agui_endpoint` is the
one-line convenience wrapper, matching the sync/recorded pair above. Both live in the
same `httpx`-dependent code path flagged in §3 — the plugin's *recorded*-stream helper
(`assert_valid_agui`) stays dependency-free; only the endpoint-validating helpers pull in
`httpx`.

---

## 7. Milestones

Mirrors the TS build order (M0–M9), each independently shippable:

| # | Milestone | Ships |
|---|---|---|
| PM0 | Scaffold & packaging | **Done.** `py/pyproject.toml` (hatchling), console script stub, and the JS-side relocation (`src/`→`js/src/`, plus `test/`, `tsconfig*.json`, `tsdown.config.mjs`, `vitest.config.ts` moving alongside it) landed ahead of schedule when the PyPI name-reservation placeholder was requested. Two corrections from this plan's original text, discovered during that work: (1) `package.json` and `.changeset/` deliberately did **not** move into `js/` — npm's `files` field cannot reach outside the directory containing `package.json`, so moving it would have required a `prepack` copy-step hack to still ship `spec/` in the npm tarball; `package.json`, `package-lock.json`, `node_modules/`, `dist/`, and `.changeset/` all stay at the repo root. (2) `scripts/` did **not** move wholesale — only the tools that actually depend on the JS build (`dist/`) or `@ag-ui/core`'s zod schemas moved to `js/scripts/`; [`generate-rule-docs.mjs`](../scripts/generate-rule-docs.mjs), which does purely repo-wide, language-neutral work (reads `spec/`, writes `docs/`, touches nothing JS-specific), stays in a `scripts/` folder at the repo root and will keep doing so once a Python implementation exists. `action/` did not move, per the original decision. |
| PM1 | Protocol grounding | **Done.** `py/scripts/derive_lib.py` (pydantic v2 introspection: unwraps `Annotated`/`Optional`, classifies `Literal`/`Union`/`List`/`Enum`/model types — the pydantic-side counterpart to `derive-lib.mjs`'s zod walking) + `py/scripts/generate_event_table.py`, producing `py/src/ag_ui_validate/protocol/event_table.py` (33 event types, checked in). `py/tests/test_protocol_drift.py` mirrors `test/protocol-drift.test.ts` exactly (enum-order coverage, per-field parametrized comparison, `SDK_VERSION` present) and runs in CI (`.github/workflows/ci.yml`'s `python-tests` job, Python 3.11). Bundled in the same PR: the `spec/event-categories.json` hoist proposed in §2 (resolves open question 6) — `CATEGORY`/`DEPRECATED` now live in one shared, hand-maintained JSON file that both `generate-event-table.mjs` and `generate_event_table.py` read, instead of a second copy that would drift. Verified the JS side is unaffected: regenerating `event-table.ts` after the hoist produced a byte-identical file. A cross-language diff (`EVENT_TABLE` dumped from both a built `dist/index.js` and the new `event_table.py`, deep-compared field-by-field) found the derivations agree on all 33 wire types and every field except one — see the finding below. |
| — | **Finding: `@ag-ui/core` and `ag-ui-protocol` aren't perfectly in sync.** `ACTIVITY_SNAPSHOT`'s `content` field is `z.record(z.any())` in `@ag-ui/core` v0.0.58 (classified `"object"`) but `typing.Any` in `ag-ui-protocol` v0.1.20 (classified `"any"`) — the Python SDK is looser here, not a derivation bug on either side (confirmed by reading each SDK's actual schema, not just the derived output). This is the first real evidence that the two official SDKs can diverge on a field's strictness even when the wire type itself matches, which has two concrete implications for later milestones: (1) PM3's schema check (`validateSchema`, driven by `EventSpec.fields`) could reject a payload on the TS side that the Python side accepts, for this field specifically — worth a fixture once PM3 lands, not before. (2) §5's parity CI (CLI-to-CLI diff across `spec/fixtures/`) needs to expect this as a known, accepted divergence for any fixture exercising `ACTIVITY_SNAPSHOT.content` with a non-object value, not treat it as a regression. No action taken beyond recording it — each SDK's derivation stays honest to what that SDK actually declares. |
| PM2 | Rule catalog | **Done.** `ag_ui_validate/rules/catalog.py` — `RuleDefinition`/`Catalog` dataclasses, `validate_catalog()`, `format_message()`, all direct ports of `catalog.ts`'s logic. `py/tests/test_catalog.py` ports every case in `test/catalog.test.ts` (spec-citation format, spec-question cross-refs, `requiresFeature` invariant, message formatting, the missing-`specUrl` rejection case, and per-rule fixture-directory coverage against `spec/fixtures/invalid/`) — 46 test cases, all passing, none needing the engine (PM3) to exist. |
| — | **Finding: `force-include` isn't materialized in editable installs.** Added `spec/` → `ag_ui_validate/spec` via `[tool.hatch.build.targets.wheel.force-include]` per §4 — confirmed correct in a real built-and-installed wheel (`spec/catalog.json` and the whole fixture corpus present, `RULES` loads with all 40 rules). But `pip install -e .` (hatchling's editable mode) points straight at `src/ag_ui_validate` on disk and does **not** materialize force-included paths, so `resources.files("ag_ui_validate") / "spec"` doesn't exist under an editable install — meaning the natural fast dev/CI loop (`pip install -e ".[dev]" && pytest`, no full build step) would have silently broken the moment any module needed `spec/` at runtime. Fixed by giving the loader a two-path lookup: the packaged resource first, falling back to the repo-relative `spec/` (four parents up from `catalog.py`) when that's absent. Verified both paths independently — an editable install exercises the fallback, a real wheel install (built + installed into a scratch venv) exercises the packaged path. This pattern (`_find_spec_dir()` in `catalog.py`) is worth reusing verbatim for any later module that needs `spec/` at runtime rather than rediscovering the same gap. |
| PM3 | Core state machine | **Done.** `engine.py` (the `Validator` class, ported from the `createValidator()` closure per §1's planned redesign), `rules/checks/{context,lifecycle,text,toolcalls,state,reasoning,transport}.py`, and `protocol/jsonpatch.py`. Confirmed mechanical and low-risk as predicted: no structural surprises beyond the closure→class shape already anticipated. Ported `test/validator/engine.test.ts` (28 cases) and `test/validator/jsonpatch.test.ts` (27 cases, the RFC 6902 appendix A examples) directly. |
| PM4 | Fixture corpus wired in | **Done, landed in the same PR as PM3** — once the engine existed, wiring the fixture corpus in was a short, natural next step rather than a separately-staged milestone; splitting it out would only have delayed catching real bugs. `py/tests/test_fixtures.py` mirrors `test/fixtures.test.ts`: 43/43 non-transport fixtures match `expected.json` exactly (7 valid + 35 invalid, plus the collective-category-coverage meta-test); 5 transport-scenario fixtures (AGUI501/505/506/507/508) were `@pytest.mark.skip`'d pending PM5 — wired in as part of PM5, see below. |
| — | **Finding: a real catalog-level language-neutrality gap, fixed.** `spec/catalog.json`'s AGUI503 `messageTemplate` hardcoded `@ag-ui/core` as literal prose (not just `{sdkVersion}` as a placeholder) — every Python AGUI503 diagnostic would have misleadingly cited the JS package name. Asked Faraz; fixed the shared template to "not in the installed AG-UI SDK v{sdkVersion}" (his choice over a per-language `{sdkName}` placeholder or leaving it). This is a real, if minor, behavior change to the TS validator's shipped message text too — regenerated `spec/fixtures/invalid/AGUI503-unknown-event-type/expected.json` and `docs/rules/AGUI503.md` via the normal tooling (zero hand-editing drift), added a changeset. **Lesson: catalog message templates need the same "would this read correctly from any SDK" scrutiny that rule logic already gets — hardcoded prose is an easy way for JS-specific assumptions to leak into supposedly language-neutral data.** |
| — | **Finding: two fixtures have permanently divergent `message` text, and that's fine.** AGUI204/AGUI502's `expected.json` embeds the *native JSON parser's* own error string (V8's, via `JSON.parse`) — Python's `json` module reports the same malformed input with different wording (e.g. "Unexpected token 'o', ..." vs "Expecting value: line 1 column 9"). No shared JSON parser exists to unify this, and manufacturing one just for message-text parity would be a worse trade than accepting the divergence. `test_fixtures.py` documents this explicitly and compares every field except `message` for `AGUI204`/`AGUI502` (and `AGUI503`, for the unrelated SDK-version-number reason above). **This is a concrete, specific case the parity CI design (§5) needs to carry forward — a naive full-diagnostic-array diff between the two CLIs would flag these 3 rules as false regressions on every run.** |
| PM5 | Transport layer | **Done.** `ag_ui_validate/transport/sse.py` and `ndjson.py` — direct, zero-dep ports of `sse.ts`/`ndjson.ts` (WHATWG SSE framing incl. `json-line-without-data-prefix`/`truncated-frame` anomaly detection; NDJSON line splitting). `ag_ui_validate/transport/__init__.py` — `TransportError`, `TransportOptions`/`TransportResult` dataclasses, `default_run_agent_input()`, `validate_body()` (byte-level tap for AGUI506/507 timing, format sniffing, recorded-mode skip logic, mid-stream-failure→AGUI508 handling), `validate_endpoint()` (httpx-based by default, lazily imported so the bare install stays dependency-free; `fetch_impl` injectable for tests/custom stacks). Ported all four TS transport test files directly: `test/transport/sse.test.ts` (14 cases), `ndjson.test.ts` (4), `endpoint.test.ts`, `recorded.test.ts` — all passing on first real run against the ported parsers/orchestration, no ported-test bugs found. The 5 previously-skipped transport fixtures (AGUI501/505/506/507/508) are now wired into `test_fixtures.py` via an async `_run_scenario()` mirroring `runScenario()` (timed byte chunks + simulated clock through `validate_body`) — full suite is 218/218 passing, 0 skipped. Verified a real built wheel (`pip wheel .`, not `python -m build` — see finding below) installs with `[transport]` and works end-to-end, and that a bare install's `validate_endpoint()` raises a clean `TransportError` (not a leaked `ImportError`) when neither `httpx` nor a custom `fetch_impl` is available. Two deliberate Python-idiom simplifications, both documented in the module docstring: no `iterateBody()`-style duck-typing helper (Python async iterables are one uniform protocol); no `AbortController`/`AbortSignal` — `timeout_ms` is passed to `fetch_impl` as `timeout_s`, and the default httpx client applies it as a connect+read+write+pool timeout, which reproduces both TS behaviors (timeout during connect → `TransportError`; timeout mid-stream → caught inside `validate_body`'s read loop as a `transport_error` diagnostic) without a separate cancellation-token mechanism. |
| — | **Finding (PM5): `python -m build` couldn't see `../spec`; `pip wheel .` could.** Building via `python -m build` (sdist → wheel) failed outright at the time — the sdist only bundled `py/`'s own tree, so `[tool.hatch.build.targets.wheel.force-include]`'s `"../spec"` had nothing to point at once packaging happened from the sdist instead of the live source tree. `pip wheel . -w dist --no-deps` (building directly against the repo checkout) was the workaround used throughout PM5–PM9's verification. **Fixed for real ahead of the first functional PyPI release** — see the next row — `python -m build` now works too. |
| — | **Fix: `py/spec` symlink resolves `python -m build` for real.** The `pypi-release.yml` release workflow itself uses `python -m build py --outdir py/dist` — i.e. the exact command PM5's finding said didn't work. Discovered this while prepping the first real PyPI release: a tag-triggered release would have failed outright (`FileNotFoundError: Forced include not found`). Root cause: `force-include`'s `"../spec"` is resolved relative to whatever directory hatchling is actually invoked from — for `pip wheel .` that's the live `py/` checkout (where `../spec` correctly reaches the repo's `spec/`), but for `python -m build`, hatchling builds the wheel from an *extracted sdist* in a temp directory that has no `../spec` sibling at all, since the sdist only ever bundled `py/`'s own tree. Fixed with a checked-in symlink, `py/spec -> ../spec` (git stores it as a real symlink, mode `120000`), plus dropping the `force-include` mapping to `"spec" = "ag_ui_validate/spec"` (no more `../`). This single change fixes *both* build paths at once: `pip wheel .` now resolves `spec` via the symlink (still inside `py/`), and — the previously-broken case — hatchling's sdist step picks up the symlinked `py/spec` like any other file during its default project-directory walk and bundles the *real file contents* into the sdist (verified: `tar tzf` on the produced sdist shows real, correctly-sized files, not a dangling symlink reference), so the later wheel-from-sdist step now finds a real `spec/` sibling to force-include from. Verified all three build paths after the fix: `pip install -e ".[dev]"` (editable, still exercises the pre-existing repo-relative fallback in `_find_spec_dir()`, unaffected), `pip wheel .` (unaffected, still correct), and `python -m build py --outdir py/dist` (the one that was actually broken — now produces a working wheel, verified by installing it into a fresh venv and running the CLI against a real fixture). **Lesson: when a monorepo needs to ship a subdirectory that lives outside a Python package's own root, prefer a checked-in symlink over a `force-include` path with `../` — the symlink is resolved once at the filesystem level and works identically regardless of which build entry point (direct wheel, sdist-then-wheel, editable) hatchling is invoked through, where a relative `../` path silently means something different in each case.** |
| PM6 | CLI | **Done.** `ag_ui_validate/cli_args.py` (hand-rolled `_FLAGS` table, direct port of `cli-args.ts` per the resolved §8 open question 3) and `ag_ui_validate/cli.py` (the I/O entry point, wired to a real `ag-ui-validate` console script via `[project.scripts]`). Also bundled in this milestone, since `cli.py` needed it and it wasn't its own numbered milestone: `ag_ui_validate/report/{pretty,json,junit,sarif}.py`, direct ports of `src/report/*.ts`. Ported `test/cli/args.test.ts` (21 cases), `test/report/reporters.test.ts` (14 cases), and `test/cli/integration.test.ts` (11 cases) — the integration suite spawns the real installed `ag-ui-validate` console script via `subprocess` against real fixture files (no build step to gate on, unlike the TS side's `dist/cli.js` skip-if-missing pattern) and all pass unmodified against the ported CLI. Full suite: 264/264 passing. Verified a real built wheel (`pip wheel .`) installs the console script correctly and produces byte-identical pretty/`--json`/`--sarif`/`--junit` output against real fixtures in a clean venv. One deliberate simplification: `_tool_version()` uses `importlib.metadata.version("ag-ui-validate")` (works under both editable and wheel installs) instead of TS's `readFileSync(../package.json)`, since Python has no equivalent of reading a sibling `package.json` at runtime. |
| PM7 | pytest plugin | **Done.** `ag_ui_validate/pytest_plugin.py` implements §6's sketch: `assert_valid_agui` (sync, dependency-free — direct port of `src/vitest/matcher.ts`'s `toBeValidAGUI`, adapted from a boolean matcher with vitest's `.not` support to pytest's assert-raises idiom) plus `validate_agui_endpoint`/`assert_valid_agui_endpoint` (async, built on `ag_ui_validate.transport`, only pulling in `httpx` at call time against a live endpoint). Added `validate_agui` (sync, returns the full `Report`) alongside `assert_valid_agui` for symmetry with the endpoint pair — not in the original sketch, but the natural counterpart and useful for tests that want to inspect the feature matrix or summary beyond pass/fail. Registered via `[project.entry-points.pytest11]`; confirmed with `pytest --trace-config` that pytest actually loads it as an active plugin (not just an importable module). Ported `test/vitest/matcher.test.ts` (adapting away the vitest-specific `.not`/`toThrow` assertions in favor of `pytest.raises`) — all 11 cases pass unmodified against the ported logic. Manually verified the two new endpoint helpers (no TS equivalent exists to port a test from, since vitest's matcher has no endpoint variant) against a mocked `fetch_impl`, both the passing and failing paths. Verified in a clean venv from a real wheel: `assert_valid_agui` works with `httpx` completely absent, confirming the recorded-stream path — the primary pytest-plugin use case — stays genuinely dependency-free. Full suite: 275/275 passing. |
| PM8 | GitHub Action parity | **Done, no code changes.** `action/run.mjs` and `action.yml` are unaffected by design: the Action always shells out to the npm package (`npx ag-ui-validate@version`, or the local `dist/cli.js` build for `version: local`/CI self-test) — there is no code path that could reach `py/`, so "parity" here just meant confirming nothing regressed. Verified two ways: (1) `git diff` from the last commit that touched `action/`/`action.yml` (`88a42fd`, pre-dating the Python port entirely) to current `main` shows zero changes to either. (2) Ran `action/run.mjs` locally against a fresh `npm run build`, reproducing both cases the `action-self-test` CI job exercises — a clean fixture (`exit 0`, `errors=0`, correct `$GITHUB_OUTPUT`/`$GITHUB_STEP_SUMMARY` content) and a violating fixture with `--sarif-file` (`exit 1`, `errors=1`, valid SARIF 2.1.0 output) — both matched exactly. No Python-specific follow-up is needed unless a future milestone wants the Action to *also* support running the Python CLI (e.g. a `version: python-local` mode), which was never in scope for this milestone and isn't implied by the plan's wording. |
| PM9 | Docs | **Done.** Resolved §8 open question 4: `docs/rules/*.md` stays shared/generated-once (no code or content changes needed — already fully data-driven, verified via `git diff` showing zero changes needed to the generator or its output). `docs/TESTING.md` became one merged guide: added a "Python:" table under each of the existing 10 numbered sections (mapped 1:1 to PM0–PM9), plus a Python quick-start in "Everything at once" and Python-specific notes in "What must never regress" — including two honest, explicitly-flagged gaps rather than papering over them: no ported `fuzz.mjs`/`purity.test.ts`/`sdk-alignment.test.ts` equivalents yet, and PM5's real-HTTP e2e verification (`npm run e2e`'s Python counterpart) was done by hand during that milestone's review but never committed as a script. Every command newly added to the merged guide was actually executed during this milestone (not just written from memory) — `pytest --trace-config`, `-k` filters, event-table regeneration + `git diff --exit-code`, a real wheel build + scratch-venv install, and the CLI/transport "by hand" one-liners — to keep the guide honest. |
| — | **Parity CI** (§5) | **Done.** `.github/workflows/parity-ci.yml` + `scripts/parity-check.mjs`. Resolved §8 open question 7 (cadence): PR-triggered only, path-filtered to `spec/**`/`js/src/**`/`py/**`, no nightly backstop — accepted tradeoff, see the open-question entry. The script runs every `spec/fixtures/valid/*.jsonl` and `spec/fixtures/invalid/*/stream.jsonl` (42 of 47; the 5 `scenario.json` transport fixtures are out of scope — neither shipped CLI has a flag for timed-chunk/injected-clock replay, so those stay covered by each language's own `fixtures.test.ts`/`test_fixtures.py`) through **both real shipped CLIs** (`dist/cli.js` and a real installed Python wheel, not test internals) with `--json`, and deep-diffs the `diagnostics` arrays with the same message-tolerant comparison `test_fixtures.py` already uses for `AGUI204`/`AGUI502`/`AGUI503`. **Real bug caught and fixed during this milestone, not by CI**: the first version of the diff script compared diagnostics via plain `JSON.stringify`, which is key-order-sensitive — since the TS object literals and the Python dict-building code (`_diagnostic_to_dict`) happen to insert fields in different orders, every single fixture "failed" on a pure ordering artifact with byte-identical semantic content. Fixed with a `canonicalJson()` helper that sorts object keys recursively before stringifying. Verified the fix three ways: (1) all 42 fixtures pass after the fix; (2) an isolated unit check of `diff()`/`canonicalJson()` against four hand-built cases (reordered keys → no diff, a real field difference → 1 problem, a length mismatch → 1 problem, a tolerated-rule message difference → no diff) confirmed the comparator's actual behavior rather than trusting the full-corpus run alone; (3) a full rehearsal in a genuinely clean environment (fresh venv, wheel-only install, no dev extras) to mirror what the CI runner actually does, not the dev `.venv` already carrying the `[dev]` extras. **Lesson: a diff tool's own correctness needs verifying independently of running it against real data once** — the ordering bug failed *every single fixture* (a false-negative direction, safer than silently passing but still would have blocked every PR on a non-issue), and it only became obvious as "probably my script, not 34 simultaneous real divergences" because the divergence messages were inspected by eye rather than trusting the fail count alone. The isolated unit check (hand-built cases with a known expected answer) is what actually confirmed the fix, not just re-running the full corpus and seeing green. |
| — | **Closed the two PM9-flagged testing gaps.** `py/tests/test_purity.py` (24 cases) and `py/tests/test_sdk_alignment.py` (11 cases) mirror `purity.test.ts`/`sdk-alignment.test.ts` — both are ordinary pytest files, so they run automatically in the `python-tests` CI job, same as the TS originals run automatically under `npm test`. `py/scripts/fuzz.py` and `py/scripts/e2e_live_server.py` mirror `fuzz.mjs`/`e2e-live-server.mjs` — both stay dev/pre-release-only scripts, not wired into CI, matching `npm run fuzz`/`npm run e2e`'s own status (neither appears in `ci.yml`). Notable porting decisions: the purity check uses usage-pattern regexes (`\bhttpx\.\w`, not a bare `\bhttpx\b`) so prose mentions in docstrings don't false-positive, same principle as the TS test's comment; it carries one narrow, explicit exception for `rules/catalog.py`'s one-time `read_text()` resource load, since Python has no build-time-JSON-import equivalent to `tsc` inlining `catalog.ts`'s `import catalogJson from "../../../spec/catalog.json"` — everything else in `catalog.py` still has to pass every other banned-pattern check. The SDK-alignment test uses `pydantic.TypeAdapter(ag_ui.core.Event)` (`ag_ui.core.Event` is a `discriminator="type"` `Annotated[Union[...]]`, discovered by inspecting the SDK directly) in place of zod's `EventSchemas.safeParse` — `.validate_python()` + catching `ValidationError` gives the same accept/reject semantics. The e2e script uses `http.server.ThreadingHTTPServer` (stdlib, no new dependency) instead of hand-rolling raw sockets; the one real design problem was reproducing Node's `res.destroy()` (an abrupt mid-stream RST) for the `/drop` scenario — solved with `SO_LINGER(0)` on the raw connection socket before `close()`, which forces a hard reset instead of a graceful FIN, then overriding `handle_error()`/wrapping `handle_one_request()` so the resulting `ConnectionError` in the server thread doesn't spam stderr. Verified: full suite 310/310 passing (up from 275); `fuzz.py` run clean across two different seeds (42, 1234); `e2e_live_server.py` run clean 6 times in a row against real loopback sockets (once for the initial pass, five more to rule out timing flakiness in the drop/buffered scenarios specifically) and confirmed to actually *catch* a failure (temporarily corrupted one `EXPECT` entry, saw the expected `FAIL`/exit-1 output, then reverted) rather than being vacuously green. `docs/TESTING.md` updated to remove the "no Python equivalent" gap language from all four spots (§2, §4, §6, "What must never regress") and add the corresponding rows; this plan doc's own PM9 entry is left as the historical record of what was true *then* — this row is the follow-up that closed it. |
| — | **Wired up `mypy`, resolving §8 open question 2.** `mypy>=1.14` added to `dev` extras; `[tool.mypy]` in `py/pyproject.toml` runs `strict = true` globally (mirrors `js/tsconfig.json`'s `strict: true`), with a `tests.*` override that turns off just the three annotation-mandate flags (`disallow_untyped_defs`, `disallow_incomplete_defs`, `disallow_untyped_decorators`, `disallow_untyped_calls`) — test bodies still get checked (`check_untyped_defs` stays on via `strict`), so real errors (wrong argument types, API misuse) are still caught, but `def test_x():` isn't required to carry a `-> None`. This mirrors what `js/tsconfig.json` actually gives TS test files for free via inference, which mypy has no equivalent for at the signature level. `scripts/` is excluded from the checked set entirely, matching `js/tsconfig.json`'s own `include` list (`["src", "test", "vitest.config.ts"]` — no `js/scripts/`). Added `py/src/ag_ui_validate/py.typed` (PEP 561 marker, confirmed it ships in the wheel) so downstream consumers of the published package get type-checking benefit from the inline annotations, not just this repo's own CI. Added `mypy src tests` as a CI step in `ci.yml`'s `python-tests` job, right before `pytest`, mirroring `npm run typecheck` running before `npm test`. **Two real findings surfaced fixing `src/` to pass `--strict`, not just annotation noise:** (1) `transport/sse.py`'s `SseEvent`/`SseComment`/`SseProblem` and `cli_args.py`'s `ParseOk`/`ParseErr` used a plain `kind: str`/`ok: bool` discriminant field — works fine at runtime, but a bare `str`/`bool` gives mypy nothing to narrow a `Union` on, so every `item.kind == "event"` / `not parsed.ok` guard left the union un-narrowed on the other side. Changed both to `Literal[...]` discriminants (zero runtime behavior change, `Literal["event"]` is still just the string `"event"`) — this is the same *shape* of fix TS gets for free from string-literal-typed discriminant fields in its own tagged unions. (2) `engine.py`'s `_make_emit`'s inner `emit` closure set `diag.event_type = current["type"]` gated only by a precomputed `about_current_event` boolean, not a direct `current is not None` check in scope — a real (if narrow) case of the code's actual null-safety invariant not being visible to the type checker; fixed with a redundant-but-explicit `if about_current_event and current is not None:` rather than changing the invariant itself. **A genuinely interesting mypy behavior discovered while designing the tests-vs-src split:** putting `strict = true` directly inside a `[[tool.mypy.overrides]]` block for `ag_ui_validate.*` (rather than at the top level) does *not* scope strict-mode to that module — mypy warns that some of strict's bundled flags (e.g. `warn_redundant_casts`) are global-only, and applies the *whole bundle* globally regardless of the override's module pattern, silently defeating the intended per-module scoping and forcing the `tests.*` override's relaxations into a no-op. Confirmed via an isolated three-file repro outside the real repo before touching the real config. The fix — `strict = true` at the top level, then explicitly re-disabling just the 3-4 flags that don't apply to test files — avoids the trap entirely. Verified: `mypy src tests` clean (0 errors, 45 files) from a fully cold `.mypy_cache`; full pytest suite still 310/310 (confirming the `Literal`/narrowing/`assert` changes didn't alter runtime behavior); the exact CI sequence (`pip install -e ".[dev]"` → `mypy src tests` → `pytest`) rehearsed end-to-end in a fresh Python 3.13 venv, not just the repo's existing dev `.venv`. |

---

## 8. Open questions

1. ~~**The `httpx` dependency (§3).**~~ **Resolved, before PM5 started:** `httpx`
   (over `aiohttp` or hand-rolling streaming HTTP over stdlib `urllib`), and scoped to
   an **optional extra** — `pip install ag-ui-validate[transport]` — rather than a
   required dependency of the whole package. `ag_ui_validate.transport` and the CLI's
   URL code path import it lazily with a clear error if it's missing; the core
   validator and the pytest plugin's recorded-stream helpers (the primary pytest-plugin
   use case, per the decision that Python is where most non-conformant emitters live)
   stay genuinely dependency-free even after `pip install ag-ui-validate` with no
   extras. Mirrors the TS side's own `vitest` peer dependency, marked optional.
2. ~~**Type checker: `pyright` or `mypy`?**~~ **Resolved: `mypy`** — the more
   conventional choice for a library shipping to PyPI for the broader Python
   community (vs. `pyright`'s appeal being mostly the `tsc`-lineage symmetry,
   a weaker argument once weighed against what most Python OSS contributors
   already expect to run). See the finding below for the actual wiring.
3. ~~**CLI flag parity (§1, `cli-args.ts` row).**~~ **Resolved, before PM6 started:**
   hand-roll the Python arg parser (`cli_args.py`'s `_FLAGS` dict, a direct port of
   `cli-args.ts`'s `Flag` table) rather than `argparse`/`click`, so error messages,
   `--help` text, and flag semantics (inline `=` values, `--rule ID=severity`, etc.)
   stay byte-for-byte identical between the two CLIs — both `argparse` and `click`
   would have meant real, hard-to-fully-close behavior drift for comparable effort.
4. ~~**Docs sharing.**~~ **Resolved, before PM9 started:** `docs/rules/*.md` and
   `docs/rules/README.md` stay generated once and shared by both languages — there
   was no per-language content to fork in the first place. `docs/TESTING.md` became
   one merged guide (Python sections added alongside the existing TS ones, organized
   by the same M0–M9/PM0–PM9 milestone numbering) rather than a separate sibling file.
5. ~~**Python version floor policy (§4).**~~ **Resolved, in the v0.3.0 release-prep
   PR:** `py/pyproject.toml`'s `requires-python` bumped from the `>=3.9`
   name-reservation placeholder to `>=3.11` — "oldest non-EOL CPython at actual
   kickoff time," matching what CI (`python-tests`) had already been running on
   since PM1, per this same open question's original framing.
6. ~~**`spec/event-categories.json` hoist (§2).**~~ **Resolved:** bundled into PM1 (§7)
   rather than done as an independent pre-port PR, since the derivation tool needed the
   same data anyway. Also unblocked moving `check-spec-links.mjs` out of `js/scripts/`
   into the root `scripts/` (§2), also done.
7. ~~**Parity CI cadence (§5).**~~ **Resolved:** PR-path-filtered only
   (`spec/**`/`js/src/**`/`py/**`), no nightly backstop — a stricter policy than
   the plan's own proposal. Accepted tradeoff: drift introduced outside a
   path-filtered PR (e.g. an SDK bump landing via `sdk-drift.yml` without touching
   `spec/**`) won't be caught until the next relevant PR runs this job, rather than
   within 24h via a nightly run. Revisit if that gap ever actually bites.

